[tool.poetry]
name = "matrix-loading"
version = "0.1.0"
description = "Loading programs for the Matrix"
authors = ["Rodrigo Aguilar <rodrigoa@student.42madrid.com>"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.1.0"
numpy = "^1.25.0"
requests = "^2.31.0"
matplotlib = "^3.7.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"